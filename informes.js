// informes.js

document.addEventListener("DOMContentLoaded", function() {
    const mensaje = document.getElementById("mensaje-informe");
    if (mensaje) {
        mensaje.innerText = "¡El módulo de informes funciona correctamente!";
    }
});
